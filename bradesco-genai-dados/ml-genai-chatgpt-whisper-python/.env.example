OPENAI_API_KEY=sua-chave-aqui

# Apos colar sua chave aqui nesse arquivo, renomeie o arquivo para .env (sem a extensão .example) e rode o script main.py para testar a integração com a API do OpenAI.
# Instale a biblioteca python-dotenv para carregar as variáveis de ambiente do arquivo .env, usando o comando: pip install python-dotenv
# Dessa forma, o arquivo .env não será exposto publicamente, mantendo sua chave de API segura.
# Adicione o .env ao seu .gitignore para evitar que ele seja comitado no repositório.